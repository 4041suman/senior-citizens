class Solution {
    public int countSeniors(String[] details) {
        int count=0;
          for(String det : details){
            String str=det.substring(11,13);
            int n=Integer.parseInt(str);
            if(n>60){
                count++;
            }
        }
    return count;
    }
}
